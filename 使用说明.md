# 股票观察器（牛来）使用说明

**版本：** V2.1
**平台：** Windows x64
**技术栈：** Electron + React

---

## 1. 产品简介

V2.0 将产品形态升级为**桌面宠物**：一只会随盈亏变表情的"牛来"常驻桌面，行情与盈亏一眼可见；原 V1.0 的完整管理能力（搜索、自选/持仓、今日盈亏、快照）通过**右键菜单 → 管理面板**完整保留。

V2.1 在 V2.0 基础上新增**宠物素材库**：通过可视化面板预览、批量导入、按情绪槽位绑定与"试穿"图片/动图/视频素材，宠物外观更换不再需要反复弹文件框。

> 我关注的股票现在涨跌多少 → 我的持仓今天赚了还是亏了 → 当前市场状态在变好还是变坏

**核心能力**

- 透明置顶桌面宠物窗口，左键拖动、位置随意
- 按当日盈亏自动切换表情：盈利 → 开心、亏损 → 沮丧、持平 → 待机
- 每 10 秒自动刷新行情，点击状态胶囊立即刷新
- 右键菜单可随时切换**底部胶囊显示内容**（金额 / 百分比 / 市值 / 持仓快讯 / 盯盘股 / 自定义文字）
- 右键菜单完整入口：自选/持仓管理、搜索添加股票、今日快照、打开数据目录
- 11:30、15:00 自动保存市场状态；非交易时间自动恢复最近一次状态
- 宠物素材库：可视化面板浏览、批量导入、按情绪绑定、一键"试穿"素材（图片/动图/视频）

**不包含**：交易下单、券商账户连接、K 线图、技术指标、财务分析、新闻资讯、股票推荐。

---

## 2. 安装与运行

### 2.1 环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（x64）、macOS 10.15 及以上（Intel / Apple 芯片） |
| Node.js | 14 及以上（仅打包与开发模式需要，运行安装包不需要） |
| 网络 | 需要访问外网行情接口 |

### 2.2 运行方式

**开发模式运行**（会先自动打包前端资源，再启动宠物窗口）：

```bash
npm install
npm start
```

**单独打包前端资源**（修改 `src/` 下的代码后）：

```bash
npm run build:renderer
```

**打包成安装包**（先执行 `npm install` 装好依赖）：

```bash
npm run dist:win   # Windows 安装包，仅能在 Windows 上构建
npm run dist:mac   # macOS 安装包，仅能在 macOS 上构建
npm run dist       # 等同于 dist:win
```

产物输出到 `release/` 目录：

```text
release/牛来-setup-1.0.0.exe      # Windows 安装包
release/牛来-1.0.0-mac-x64.dmg    # macOS Intel 芯片
release/牛来-1.0.0-mac-x64.zip    # macOS Intel 芯片（免安装）
release/牛来-1.0.0-mac-arm64.dmg  # macOS Apple 芯片
release/牛来-1.0.0-mac-arm64.zip  # macOS Apple 芯片（免安装）
```

Windows 安装包可选择安装路径，默认创建名为「股票观察器」的桌面快捷方式；macOS 版把 `.app` 拖入「应用程序」即可，免安装的 `.zip` 解压后双击运行。安装包版本的数据目录与开发模式不同，详见 2.3。

**提示**：macOS 的 `.app` / `.dmg` 只能在 macOS 机器上构建，Windows 上执行 `npm run dist:mac` 会报错；若没有 Mac 可用，见 2.4 的 GitHub Actions 自动出包。未签名的 macOS 包首次打开需**右键 →「打开」**放行。

### 2.3 数据存放位置

所有数据以 JSON 文件保存在系统用户目录，不依赖数据库：

```text
开发模式：C:\Users\<用户名>\AppData\Roaming\stock-watcher-widget\Data\
安装版本：C:\Users\<用户名>\AppData\Roaming\牛来\Data\
```

也可以在**宠物右键菜单 →「打开数据目录」**，或在管理面板底部点击**「打开数据目录」**直接跳转，无需手工查找。

macOS 对应路径为 `~/Library/Application Support/stock-watcher-widget/Data/`（开发模式）与 `~/Library/Application Support/牛来/Data/`（安装版本），同样可通过「打开数据目录」跳转。

### 2.4 自动出包（GitHub Actions）

仓库内的 `.github/workflows/release.yml` 会分别在 Windows 与 macOS 云端机器打包，一次性产出两个平台的安装包：

- 推送 tag 触发：`git tag v1.0.0 && git push origin v1.0.0`，构建完自动创建 GitHub Release 并附上全部安装包；
- 手动触发：GitHub 仓库页面 → Actions → **Release** → `Run workflow`，产物在流水线页面下载，不会创建 Release。

---

## 3. 桌面宠物（主界面）

### 3.1 形态

启动后桌面出现一只约 240 × 300 的透明小牛角色，始终置顶、不占任务栏图标。

```text
   ┌────────────┐
   │  🐮(随盈亏变表情) │   ← 透明置顶角色，可整窗左键拖动
   │            │
   └────────────┘
    [● 今日 亏损 -123.45]  ← 底部状态胶囊（点击立即刷新）
```

| 要素 | 说明 |
|---|---|
| 角色本体 | 按当日盈亏播放动画：盈利轻快弹跳 / 亏损低落 / 持平缓慢浮动 |
| 底部胶囊 | 状态点 + 一行摘要文字；默认"今日 盈亏状态 金额" |
| 气泡 | 点击胶囊后短暂出现一句行情播报 |

### 3.2 鼠标交互

| 操作 | 行为 |
|---|---|
| **左键按住拖动** | 移动宠物窗口（松开即固定在新位置） |
| **单击胶囊** | 立即刷新一次行情，并弹出行情气泡 |
| **右键角色或胶囊** | 打开原生功能菜单（见 3.6） |

> 宠物窗口**没有**系统边框与关闭按钮；关闭宠物窗口等于退出程序。结束程序请使用右键菜单「退出」。

### 3.3 表情状态联动

每次刷新后按当日总盈亏切换表情：

| 当日总盈亏 | 状态 | 表情 / 动画 |
|---|---|---|
| > 0 | 盈利 | 开心（轻快弹跳） |
| < 0 | 亏损 | 沮丧（低落） |
| = 0 | 持平 | 待机（缓慢浮动） |

- 网络异常、行情更新失败、展示快照恢复数据时**不切换表情**，沿用上次状态。
- 可在**右键 → 预览表情**里手动固定某个表情；选择「恢复自动（按行情）」后回到自动联动。

### 3.4 宠物外观（按情绪更换形象，支持图片与视频）

桌宠形象**按情绪分别替换**：待机（持平）、开心（盈利）、沮丧（亏损）可各自绑定一张图片、一个动图或一段视频，互不影响。换好后表情仍会随盈亏自动切换。

**支持的素材**

| 类型 | 格式 | 说明 |
|---|---|---|
| 静态图 | png / jpg/jpeg | 单帧形象 |
| 动图 | png(APNG) / webp / gif | 自动循环播放 |
| 视频 | webm / mp4 / mov / m4v | 自动静音循环 |

视频请优先使用**带透明背景的 webm**（透明动画），普通 mp4 没有透明通道，会以不透明底色方框显示。

**更换步骤（快速方式）**

1. 右键宠物 →「宠物外观」
2. 点「更换待机（持平）显示… / 更换开心（盈利）显示… / 更换沮丧（亏损）显示…」
3. 在文件框里选中你的图片或视频 → 桌宠立即换上新形象

**推荐做法**：右键 →「宠物外观」→「打开素材库管理…」，在可视化面板中预览并绑定素材，操作更直观（见 3.4.1）。

想换回默认：在素材库槽位卡片上点「移除」，或菜单「恢复默认显示」→ 选对应情绪或「全部恢复」。

**其他说明**

- 素材会复制到程序素材目录保存，重启后仍生效；原文件删了也不影响
- 同一个文件可被多个情绪同时使用
- 素材等比缩放居中显示，不会裁切变形
- 想清理素材：右键 →「宠物外观」→「打开素材目录」，删除对应文件即可；对应绑定会自动回退默认形象，不影响运行
- 素材加载失败会自动回退默认形象，桌宠不会消失

### 3.4.1 素材库管理面板（可视化素材选择器）

右键宠物 →「宠物外观」→「打开素材库管理…」打开素材库小窗。它是管理素材的推荐入口，可与宠物窗口同时使用——**素材或试穿效果在宠物窗口即时生效**。窗口自上而下分三块：

1. **情绪槽位区**：待机 / 开心 / 沮丧三张卡片，各自显示当前绑定素材的缩略图与文件名；
   - 点缩略图可在下方网格中定位该素材；点「移除」恢复该情绪的默认形象
2. **素材库网格**：展示全部已导入素材，图片即时显示缩略图，**视频悬停或选中时循环播放预览**；
   - 卡片左上角彩色角标 = 已绑定情绪（待机=蓝 / 开心=黄 / 沮丧=红）；卡片下方为文件名与类型标签（图片 / 视频）
3. **底部操作条**：点选某个素材后可使用：

| 按钮 | 行为 |
|---|---|
| 设为待机 / 设为开心 / 设为沮丧 | 一键把该素材绑定到对应情绪；已是该情绪显示的按钮会变为"已设××"（不可重复点） |
| 试穿到宠物 | **不修改绑定**，宠物窗口立即以该素材播放，方便先看效果再决定；点「停止试穿」或关闭素材库即恢复原样 |
| 重命名 | 修改素材文件名（扩展名类型不可改变，例如图片不能改成视频） |
| 删除素材 | 二次确认后删除文件；绑定了该素材的情绪自动恢复默认，正在试穿会自动停止 |

**常用操作**

- 右上角「导入素材…」：一次多选批量导入，格式不支持或超过 300MB 的文件自动跳过并提示数量
- 「刷新」：手动重新读取素材目录与绑定（素材目录被手工增删文件后使用）
- 想删除素材又找不到入口？先在网格点选该素材，再点「删除素材」

**与右键菜单互通**：素材库面板、右键菜单快速更换、直接删除素材文件，任意入口的改动都会即时同步到宠物、素材库与右键菜单，无需重启。绑定关系保存在 `Data\mood-bindings.json`；素材文件本身存于素材目录，可用右键菜单「宠物外观 → 打开素材目录」直达。

### 3.5 底部状态胶囊（显示内容可配置）

V2.0 起胶囊默认显示"今日盈亏金额"，可随时通过**右键 → 底部显示内容**菜单切换显示项，选择会**持久化保存**（重启后保持）。

| 显示项 | 示例 | 说明 |
|---|---|---|
| 今日盈亏（金额） | `今日 亏损 -123.45` | 默认项 |
| 盈亏金额 + 当日涨跌幅 | `今日 亏损 -123.45（-1.23%）` | 金额与百分比同屏 |
| 当日涨跌幅（百分比） | `今日 亏损 -1.23%` | 仅百分比（按持仓总成本计算） |
| 持仓总市值 / 总成本 | `市值 ¥2.5万 / 成本 ¥2.6万` | 金额自动缩写为亿/万 |
| 持仓数 · 自选数 | `持仓 2 · 自选 5` | 数量统计 |
| 持仓涨跌轮播 | `贵州茅台 ¥1,720.00 +0.35%` → … | 每 3.2 秒逐只轮播持仓行情 |
| 盯盘股行情 | `中科曙光 ¥39.80 +2.10%` | 固定盯一只自选/持仓股 |
| 自定义文字 | `努力工作，好好搬砖` | 完全自定义的固定文字 |

**操作路径**

1. 右键宠物 →「底部显示内容」
2. 点选任一显示项，胶囊立即切换

**自定义文字**

- 选择「输入自定义文字…」（或选中"自定义文字…（未设置）"）后在输入框中填写，最多 40 字
- 保存后胶囊固定显示这段文字，不再随行情变化
- 想改回来：右键 →「底部显示内容」→「编辑自定义文字…」改内容，或直接点选其他显示项

**盯盘股**

- 选择「选择盯盘股（自选/持仓）」，从列表里勾选一只
- 勾选后胶囊自动切到"盯盘股行情"模式，显示该股名称、现价与当日涨跌幅

**说明**

- 文本超长时胶囊自动省略（…），鼠标悬停可看全文
- 持仓/盯盘模式在非交易时段沿用最近行情照常显示
- 百分比 = 当日总盈亏 ÷ 持仓总成本；无成本数据的持仓会使该模式下显示 `--`

### 3.6 右键菜单全览

| 菜单项 | 行为 |
|---|---|
| 牛来 · 今日盈亏（只读） | 当前盈亏状态、时段、持仓/自选数量概要 |
| 自选 / 持仓管理 | 打开管理面板 |
| 搜索添加股票 | 打开管理面板（定位搜索） |
| 今日快照 | 打开管理面板（定位快照） |
| 打开数据目录 | 在资源管理器中打开本地数据文件夹 |
| **底部显示内容** | 子菜单：切换胶囊显示项 / 选择盯盘股 / 编辑自定义文字（见 3.5） |
| **宠物外观** | 子菜单：打开素材库管理（推荐）/ 更换待机·开心·沮丧显示 / 恢复默认 / 打开素材目录（见 3.4、3.4.1） |
| 预览表情 | 子菜单：手动固定待机/开心/沮丧，或恢复随行情自动 |
| 退出 | 结束进程（含关闭管理面板） |

---

## 4. 管理面板（V1.0 完整功能）

桌面小组件时期的功能全部保留，改由宠物右键菜单打开：

**打开方式**：右键宠物 →「自选 / 持仓管理」（或「搜索添加股票」「今日快照」），面板为单实例，已打开时再次点击会聚焦复用。

面板窗口初始尺寸 1180 × 860，最小 980 × 720，可自由拉伸。**关闭面板不影响宠物驻留**；宠物与面板共用同一套本地数据与行情逻辑。

### 4.1 股票搜索

在搜索框输入**股票名称**或**股票代码**，支持模糊匹配：

| 输入示例 | 返回结果 |
|---|---|
| `中科` | 中科曙光、中科信息、中科创达、中科江南 …… |
| `603019` | 代码包含 603019 的股票 |
| `688` | 代码包含 688 的股票 |

**使用要点**

- 输入后**自动检索**，无需按回车，带 400 毫秒防抖
- 默认展示前 10 条结果，超出可在表格区域内滚动
- 已加入自选的股票，按钮变为 **「已加入自选」** 且不可点击
- 已加入持仓的股票，按钮变为 **「修改持仓」**
- 股票名称后会显示 `自选` / `持仓` 标签

**操作按钮**

| 按钮 | 行为 |
|---|---|
| 加入自选 | 加入自选股列表；已存在则不重复添加 |
| 加入持仓 | 弹出持仓录入窗口；已存在则直接进入编辑 |

### 4.2 自选股

用于保存长期关注但不一定持有的股票。

列表字段：序号、股票名称、股票代码、当前价、当前涨幅、较开盘价涨跌幅、状态。

**计算口径**

```text
当前涨幅      = (当前价格 - 昨日收盘价) / 昨日收盘价 × 100%
较开盘价涨跌幅 = (当前价格 - 今日开盘价) / 今日开盘价 × 100%
```

**颜色规则**

| 涨跌情况 | 文字颜色 |
|---|---|
| 上涨 | 红色 |
| 下跌 | 绿色 |
| 持平 | 默认灰白色 |

### 4.3 持仓股

用于记录实际持有的股票，并计算今日盈亏。

在自选股基础上增加：持股数、今日盈亏、操作（编辑持仓）。

**录入流程**

1. 搜索到目标股票，点击「加入持仓」
2. 在弹出窗口中填写：
   - **股票名称 / 股票代码**：自动带出，不可修改
   - **持股数量**：必填，必须大于 0
   - **持仓成本价**：选填，用于底部胶囊"涨跌幅"等口径的计算
3. 点击「保存」

> 同一只股票只能有一条持仓记录。重复添加时会直接更新数量与成本价。

**修改持仓**：在持仓列表点击「编辑持仓」，或在搜索结果点击「修改持仓」。

### 4.4 今日盈亏

**计算口径**

```text
单只股票今日盈亏 = (当前价格 - 昨日收盘价) × 持股数量
今日总盈亏       = 所有持仓股票今日盈亏之和
```

**示例**

```text
昨日收盘价 20 元，当前价格 21 元，持股 1000 股
→ (21 - 20) × 1000 = +1000 元
```

**盈亏状态**

| 条件 | 状态 |
|---|---|
| 今日总盈亏 > 0 | 盈利 |
| 今日总盈亏 < 0 | 亏损 |
| 今日总盈亏 = 0 | 持平 |

面板内以背景色实时反馈（盈利红色 / 亏损绿色 / 持平黑色，带 0.6 秒渐变）；宠物侧对应表情状态见 3.3。

### 4.5 牛 / 熊动画（面板内）

面板动画区根据**相邻两次刷新的盈亏变化**播放动画：

| 判断条件 | 行为 |
|---|---|
| 本次总盈亏 > 上次总盈亏 | 播放 **牛动画**（红色，向上冲撞 + 上升射线） |
| 本次总盈亏 < 上次总盈亏 | 播放 **熊动画**（绿色，向下压 + 下沉射线） |
| 本次总盈亏 = 上次总盈亏 | 不播放新动画，显示持平图标 |

单次动画时长约 2.4 秒。**不会播放动画的情况**：非交易时间、网络异常更新失败、展示快照恢复数据、个别股票行情缺失导致盈亏不可比。

> 注：宠物表情只跟随**当日盈亏状态**（赚/亏/平），不做相邻两次的"牛上攻/熊下压"动画。

### 4.6 行情刷新

- 程序启动后**立即刷新一次**
- 之后**每 10 秒刷新一次**，时间对齐到整 10 秒（`00 / 10 / 20 / 30 / 40 / 50` 秒）
- 自选股与持仓股**合并为一次批量请求**，不逐只查询
- 宠物与面板共用同一刷新周期与行情数据

每次刷新更新：当前价格、当前涨幅、较开盘价涨跌幅、单只股票今日盈亏、今日总盈亏、盈亏状态、宠物表情、牛/熊动画状态。

### 4.7 市场交易时间

系统自动识别 A 股交易状态，管理面板顶部徽标与宠物右键菜单头部实时显示：

| 状态 | 时间段（周一至周五） |
|---|---|
| 开盘前 | 00:00 – 09:30 |
| 上午交易 | 09:30 – 11:30 |
| 午间休市 | 11:30 – 13:00 |
| 下午交易 | 13:00 – 15:00 |
| 收盘后 | 15:00 – 24:00 |
| 非交易日 | 周六、周日，以及配置的节假日 |

仅在**上午交易**和**下午交易**两个时段拉取实时行情。

### 4.8 市场状态快照

每个交易日自动保存两次市场状态：

| 时间 | 保存内容 |
|---|---|
| **11:30** | 日期、时间、今日总盈亏、盈亏状态、市场状态、自选股与持仓股完整行情 |
| **15:00** | 同上（当天最终状态） |

**补拍机制**：如果 11:30 或 15:00 时程序没有运行，下次启动后会自动补拍当时的状态。

**非交易时间恢复优先级**

```text
1. 今天 15:00 快照
2. 今天 11:30 快照
3. 最近一个已交易日的 15:00 快照
4. 最近一个已交易日的 11:30 快照
```

**典型场景**

| 场景 | 行为 |
|---|---|
| 12:00 打开程序 | 读取当天 11:30 快照 |
| 20:00 打开程序 | 读取当天 15:00 快照 |
| 次日 08:00 打开程序 | 读取最近一个交易日 15:00 快照 |

快照恢复时，面板状态列显示「非交易时间，展示最近保存状态」；宠物沿用该状态显示盈亏，不切换表情、不播牛熊动画。

---

## 5. 数据与配置

### 5.1 数据文件

| 文件 | 内容 |
|---|---|
| `watchlist.json` | 自选股列表 |
| `holdings.json` | 持仓股列表 |
| `stocks.json` | 股票名称缓存（离线回退用） |
| `daily-status.json` | 当前交易日的 11:30 / 15:00 快照 |
| `daily-history.json` | 历史快照归档（滚动保留最近 30 个交易日） |
| `market-config.json` | 交易日历配置 |

**自选股结构**

```json
{
  "code": "603019",
  "name": "中科曙光",
  "market": "sh"
}
```

**持仓股结构**

```json
{
  "code": "603019",
  "name": "中科曙光",
  "market": "sh",
  "quantity": 1000,
  "costPrice": 25.50,
  "addedAt": "2026-09-05T07:12:33.000Z"
}
```

**每日状态结构**

```json
{
  "date": "2026-09-05",
  "morning": {
    "time": "11:30",
    "savedAt": "2026-09-05T03:30:00.000Z",
    "profit": 1250.00,
    "status": "盈利",
    "marketPhase": "午间休市"
  },
  "afternoon": {
    "time": "15:00",
    "savedAt": "2026-09-05T07:00:00.000Z",
    "profit": 850.00,
    "status": "盈利",
    "marketPhase": "收盘后"
  }
}
```

### 5.2 交易日历配置

A 股存在节假日与调休，默认按「周一至周五 + 固定节假日」判断交易日。如需精确适配，可编辑 `Data\market-config.json`：

```json
{
  "holidays": ["2026-10-05", "2026-10-06", "2026-10-07"],
  "extraTradingDays": ["2026-09-27"],
  "fixedHolidays": ["01-01", "05-01", "10-01", "10-02", "10-03"]
}
```

| 字段 | 说明 |
|---|---|
| `holidays` | 额外休市日期，格式 `YYYY-MM-DD` |
| `extraTradingDays` | 调休补班但需要交易的周末，格式 `YYYY-MM-DD` |
| `fixedHolidays` | 每年固定休市日期，格式 `MM-DD` |

修改后重启程序生效。

### 5.3 自动备份

写入数据时自动生成备份，无需手工维护：

| 文件 | 说明 |
|---|---|
| `<文件名>.bak` | 上一次成功写入的版本 |
| `<文件名>.corrupt-<时间戳>` | 损坏且无法恢复时被隔离的原件 |

---

## 6. 异常处理

### 6.1 行情接口异常

- **不清空**已显示的数据，保留上一次有效行情
- 宠物表情不切换、胶囊沿用上次显示
- 管理面板顶部显示 **「网络异常」** 徽标，提示条变黄说明原因
- 下一个 10 秒周期自动重试，恢复后继续正常刷新
- 异常期间**不播放**牛熊动画

### 6.2 单只股票行情失败

- 其他股票正常更新
- 该股票状态列显示「行情获取失败(价格沿用上次,不计入盈亏)」
- 该股票**不参与本次盈亏计算**
- 若参与计算的股票集合发生变化，本次也不播放牛熊动画（避免虚假信号）

### 6.3 本地数据损坏

- 自动尝试从 `.bak` 备份恢复，并在面板黄色提示条中告知
- 无法恢复时，原件被隔离为 `.corrupt-<时间戳>`，程序以空数据正常启动
- 全程不影响软件启动与行情刷新

### 6.4 网络断开

- 管理面板显示「网络异常」
- 保留最后一次有效行情，宠物正常驻留
- 不播放新的牛熊动画、不切换表情
- 网络恢复后自动继续刷新

---

## 7. 常见问题

**Q：桌面出现的是牛，我想看原来完整的自选/持仓表格？**

右键宠物 →「自选 / 持仓管理」打开管理面板，全部功能与 V1.0 一致。

**Q：底部胶囊的文字想换一种，怎么操作？**

右键宠物 →「底部显示内容」，点选任意显示项即可，选择会自动保存。详见 3.5。

**Q：自定义文字在哪里输入？**

右键 →「底部显示内容」→「输入自定义文字…」，在弹出输入框填写后保存，最多 40 字。

**Q：为什么打开后行情不动，显示"暂无最近保存状态"？**

非交易时间（午间休市、收盘后、周末、节假日）程序不会拉取实时行情，而是恢复最近一次保存的快照。如果本地还没有任何快照，就会显示暂无。等到交易时间再打开，或先添加自选/持仓股并让程序在 11:30 / 15:00 保存过一次即可。

**Q：今天不是交易日，想调试怎么办？**

在 `Data\market-config.json` 的 `extraTradingDays` 中加入今天的日期，重启后即按交易日处理：

```json
{ "extraTradingDays": ["2026-09-05"] }
```

注意周末行情接口返回的仍是上一交易日收盘价，总盈亏不会变化，因此动画不会触发，但列表会显示真实价格。

**Q：自选股/持仓加错了怎么办？**

当前版本尚未提供删除入口。可手工编辑 `Data\watchlist.json` / `Data\holdings.json` 删除对应条目，然后重启程序。持仓的**数量与成本**可在面板点「编辑持仓」修改。

**Q：数据会丢失吗？**

数据保存在本地 JSON 文件中，程序重启、重装系统盘以外的操作都不会丢失。每次写入都会生成 `.bak` 备份。卸载程序不会自动删除 `Data` 目录，需手工清理。

**Q：素材在哪里统一管理？**

右键宠物 →「宠物外观」→「打开素材库管理…」。面板里可预览、批量导入、绑定、试穿、重命名、删除，改动自动同步到宠物与右键菜单。绑定关系保存在 `Data\mood-bindings.json`，素材文件在素材目录（面板或菜单「打开素材目录」可直达）。

**Q：行情数据来源是什么？**

- 搜索：东方财富公开搜索接口
- 行情：腾讯行情公开接口

均为免费公开数据源，未引入 Token 鉴权。若接口限流或临时不可用，程序会自动降级保留上次数据。

---

## 8. 版本说明与当前限制

### V2.1 新增

- 宠物素材库：可视化素材管理面板（缩略预览 / 批量导入 / 按情绪槽位绑定 / 移除恢复默认）
- 素材"试穿"：不修改绑定，在宠物窗口即时预览素材效果，可随时停止
- 素材删除与重命名：二次确认与类型校验，绑定、试穿状态自动联动
- 绑定关系上移主进程统一管理（`Data\mood-bindings.json`），宠物 / 素材库 / 右键菜单多窗口实时同步

### V2.0 新增

- 桌面宠物形态：透明置顶窗口、左键拖动、表情随盈亏变化
- 底部状态胶囊：8 种可配置显示内容（含自定义文字、盯盘股、持仓轮播）
- 右键原生菜单：今日盈亏概要 + 管理入口 + 表情预览 + 退出
- 宠物主窗口 + 单实例管理面板窗口结构

### 尚未提供（规划中）

- 删除自选股 / 删除持仓
- 拖动调整自选顺序
- 开机自启动
- 最小化到系统托盘
- K 线图、分时图
- 历史盈亏与每日盈亏曲线
- 自选股涨跌排名

### 其他已知约束

- 交易日历需手动维护节假日，未接入外部交易日历服务
- 不区分用户角色，为单用户桌面应用
- 仅支持 Windows x64
